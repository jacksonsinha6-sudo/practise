# practise

## Check the webpage quickly

Run the one-command checklist script:

```bash
./check.sh
```

This validates:
- HTML parsing (`index.html`)
- JavaScript parsing (`script.js`)
- local HTTP serving and `200 OK` response for `index.html`
